"use strict";

/**
 *　⚠️ DOMが操作できる様になるまで待つ必要があるため、
 * `document.addEventListener("DOMContentLoaded",function(){ ... });` の中に書く。。
 */

document.addEventListener("DOMContentLoaded",function(){

    /**
     * (問題 1) windowオブジェクト
     * `window` / `document` の中身をconsoleに出力しよう。
     */

    /**
     * (問題 2) querySelector()
     * 1. document.querySelector()を使って `.main-title`を取得し、変数 `mainTitle`に代入しよう。
     * 2. また、consoleに`mainTitle`を出力しよう。
     */


    /**
     * (問題 3) 要素の操作
     * 1. `element.innerHTML` で(問題 2)で取得した要素の中身を好きな文字列に書き換えよう。
     * 2. また、`element.classList.add()` で class `big-title`を追加しよう。
     */


    /**
     * (問題 4) querySelectorAll()
     * 1. querySelectorAll()を使って `.section`要素を全て取得し、変数に代入しよう。
     * 2. また、consoleに出力しよう。
     */

    /**
     * (問題 5) for
     * 1. (問題 4)で取得した要素配列を `for` を使って一つ一つ consoleに出力しよう。
     * 2. また、`element.style.color` を `#ff0000`に変更しよう。
     * 3. また、`element.classList.add()` で class `block`を追加しよう。
     */



});
